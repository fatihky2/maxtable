1. Runnig

a. Create a table to store the file meta information for the whole storage cluster. 
create table tran_metas (varchar file_alias_name, varchar file_true_name, varchar file_location, int file_size,...varchar owner....)

b. Insert one record for the application or file applied by some one user (User1)
insert into tran_metas (Peter, /home/transoft/jeff, 127.0.0.1, 10000k)

c. Insert one record for the application or file applied by other user (User2)
insert into tran_metas (Jimmy, /home/transoft/jeff, 127.0.0.1, 10000k)

d. Query one file
select tran_metas (Peter)
Return: "Peter, /home/transoft/jeff, 127.0.0.1, 10000k"

e. Query one file
select tran_metas (Jimmy)
Return: "Jimmy, /home/transoft/jeff, 127.0.0.1, 10000k"

2. Building

./clean.sh
./build.sh

./startClient
./startMaster
./startRegion

